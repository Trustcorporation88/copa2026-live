{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"pnpm api:dev\" \"pnpm web:dev\"",
    "api:dev": "pnpm --filter @workspace/api-server run dev",
    "web:dev": "pnpm --filter @workspace/copa2026 run dev",
    "build": "pnpm --filter @workspace/copa2026 run build && pnpm --filter @workspace/api-server run build",
    "start": "cross-env NODE_ENV=production PORT=5000 pnpm --filter @workspace/api-server run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  }
}
